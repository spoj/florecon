[package]
name = "florecon"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4"
serde = { version = "1", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]

[dev-dependencies]
csv = "1.3"
chrono = "0.4"
parquet = "51.0"
serde_json = "1"
