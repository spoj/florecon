[package]
name = "solver"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
# `cdylib` is the wasm artifact; `rlib` lets the native `harness` link it.
crate-type = ["cdylib", "rlib"]

[dependencies]
florecon.workspace = true
serde = { version = "1", features = ["derive"] }
